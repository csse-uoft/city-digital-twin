# GraphDB endpoint reachable from the backend container.
ENDPOINT_URL=http://your-graphdb-host:7200/repositories/your-repository

# Backend API URL reachable from the user's browser; embedded at build time.
REACT_APP_API_URL=http://localhost:3000
